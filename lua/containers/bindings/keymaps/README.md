# Keymaps

No default keymaps are defined by nvim-containers — all functionality is exposed
via user commands (see [`/docs/BINDINGS.md`](../../../../docs/BINDINGS.md)).

Add keymap modules here if/when default keymaps are introduced.
