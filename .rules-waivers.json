{
  "DEP-02": "all five findings (docker/nerdctl/podman/wsl adapters' exec_in_container.lua, bindings/usrcmds/container_commands_buffer.lua) are the else-branch of the same `if vim.fn.has(\"nvim-0.11\") == 1 then jobstart(..., {term=true}) else termopen(...) end` gate, each with `---@diagnostic disable-next-line: deprecated` immediately above -- correctly version-gated fallbacks, not unguarded deprecated calls."
}
